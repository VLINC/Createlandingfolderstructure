Build out the Home page (route "/") only.

Hard rules:
- Do NOT touch build/deploy/config files.
- Only edit/create files under /src (components + pages).
- Keep React Router v6 structure as-is.
- Keep components small and reusable.
- Use semantic HTML + accessibility (skip link target, headings in order, aria labels).
- Mobile-first responsive design.

Home page requirements:
1) Header
   - Logo (text is fine), nav links: Services, Reviews, Assistance, Apply
   - CTA button: "Book Now" scrolls to the BookingFlow section
   - Sticky header on scroll

2) Hero
   - H1: "Professional Home Cleaning in the Kansas City Metro"
     (Do NOT specify Missouri or Kansas in the headline.)
   - Subtext: short value prop
   - Primary CTA: "Book a Cleaning"
   - Secondary CTA: "See Services"
   - Background: subtle gradient; include trust row (e.g., "Insured • Background-checked • Eco-friendly options")

3) Features section
   - 3–6 feature cards with icons (inline SVG ok)
   - Clear benefits: Deep cleaning, Move-in/out, Eco-friendly, Senior/Disability friendly, Transparent pricing

4) Assistance section
   - Explain Affordable Access / assistance (high level)
   - CTA to "/assistance" and "/apply"

5) BookingFlow section
   - Show steps: Choose service → Pick date → Confirm → Cleaner arrives
   - Include a button linking to booking (or placeholder if booking is elsewhere)

6) Gallery section
   - Simple responsive grid with placeholder images (use div placeholders if no assets)

7) Testimonials
   - 3 testimonials with names + neighborhood (placeholder ok)
   - Include star rating UI

8) Footer
   - Links + contact email + service area summary (say "Kansas City Metro" or "Kansas City & nearby areas")
   - Add small legal text: "© {current year} Pro Clean"

Deliverables:
- Update/create these files only:
  /src/components/Header.tsx
  /src/components/Hero.tsx
  /src/components/Features.tsx
  /src/components/Assistance.tsx
  /src/components/BookingFlow.tsx
  /src/components/Gallery.tsx
  /src/components/Testimonials.tsx
  /src/components/Footer.tsx
  /src/components/SEO.tsx
  /src/components/SkipToContent.tsx
- If any component doesn’t exist, create it.
- Provide the full code for each file you changed.